name: Node CI

on:
  push:
    branches: ['**']
    paths: ['node-js/**']
  pull_request:
    branches: ['**', '!main']
    paths: ['node-js/**']

jobs:
  call:
    uses: ./.github/workflows/reusable-node.yml
